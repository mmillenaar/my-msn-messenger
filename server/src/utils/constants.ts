export enum MessageStatus {
    SENT = 'sent',
    RECEIVED = 'received',
    READ = 'read'
}

export enum ContactRequestActions {
    ACCEPT = 'accept',
    RECEIVE = 'receive',
    REJECT = 'reject',
    SEND = 'send'
}